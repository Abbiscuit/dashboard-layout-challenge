import { SignupView } from 'src/app';

import Layout from '@/components/layout/Layout';

const ViewAndLogic = () => {
  return (
    <Layout>
      <SignupView />
    </Layout>
  );
};

export default ViewAndLogic;
